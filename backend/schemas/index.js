import notifications from "./notifications"
import requests from "./requests"
import service from "./service"
import users from "./users"

export const schemaTypes = [users, service, requests, notifications]

